.container-fluid
  .row
    .col-lg-12
      h1.page-header Rooms
      .div
        a#addRoomButton.btn.btn-primary(href='#' type='button' data-toggle='modal' data-target='#roomModal')
            i.fa.fa-plus.fa-fw
            |  Add Room        
  .row
    .col-lg-12
      .card  
        .div#roomList
            
#roomModal.modal.fade(tabindex='-1' role='dialog' aria-labelledby='myModalLabel')
  .modal-dialog(role='document')
    .div#modalContent
      .loader
  
script.

  $(function() {
  
    // Get Rooms
    $.get( "/api/rooms", data => {
      $('#roomList').html(data);
    });
  
    // Add Room
    $('#addRoomButton').click(() => {
      $.get('/rooms/new', html => {          
        $('#modalContent').html(html);
      })
    });

  });
  

  
