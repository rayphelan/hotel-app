.modal-content
  .modal-header
    h4.modal-title Delete Service
  .modal-body              
    if bookings.length > 0
      .alert.alert-danger 
        p There #{bookings.length==1? 'is':'are'} currently <strong>#{bookings.length} Booking#{bookings.length!=1? 's':''}</strong> referencing this Service.
        p In order to enforce Referential Integrity, you can not delete this Service unless you first delete any Bookings referencing it.
    else
      p Are you sure you want to delete this service?           
  .modal-footer
    .div#errorMessage
    button.btn.btn-default(type='button' data-dismiss='modal') Cancel
    if !bookings.length > 0
      button#saveBtn.btn.btn-danger(type='button' onclick="deleteServiceRequest('"+id+"')") Delete