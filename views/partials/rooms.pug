div
  table.table.table-hover
    thead
      tr.table-danger
        th(scope='col') Room
        th(scope='col') Room Type
        th(scope='col') Price 
        th(scope='col') Price Per
        th.text-right(scope='col') Operations               
    tbody
      each room in rooms
        tr(id='row'+room._id)
          td #{room.name}
          td #{room.roomtype.type}
          td #{room.price}
          td #{room.pricePer}
          td.text-right
            a.btn.btn-warning(onclick='editRoomFunction("'+room._id+'")' type='button' data-toggle='modal' data-target='#roomModal')
              i.fa.fa-pencil.fa-fw
              |  Edit
            a.btn.btn-danger(onclick='deleteRoomFunction("'+room._id+'")' type='button')
              i.fa.fa-times.fa-fw
              |  Delete       
